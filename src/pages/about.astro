---
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "About Me";
const happy = true;
const finished = true;
const goal = 3;

const identity = {
  firstName: "Sarah",
  country: "Canada",
  occupation: "Technical Writer",
  hobbies: ["photography", "birdwatching", "baseball"],
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Writing Docs"];

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase";
---

<style define:vars={{ skillColor, fontWeight, textCase }}>
  h1 {
    color: purple;
    font-size: 4rem;
  }
  .skill {
    color: var(--skillColor);
    font-weight: var(--fontWeight);
    text-transform: var(--textCase);
  }
</style>

<BaseLayout pageTitle={pageTitle}>
  <h2>...y mi nuevo sitio Astro!</h2>

  <p>
    Estoy trabajando en el tutorial introductorio de Astro. 
    Esta es la segunda página de mi sitio web y es la primera que creé yo mismo.
  </p>

  <p>
    ¡Este sitio se actualizará a medida que complete más partes del tutorial, 
    así que siga visitándolo y vea cómo va mi viaje!
  </p>

  {happy && <p>¡Estoy feliz de estar aprendiendo Astro!!</p>}

  {finished && <p>¡Terminé este tutorial!</p>}

  {
    goal === 3 ? (
      <p>Mi objetivo es terminar en 3 días..</p>
    ) : (
      <p>My goal is not 3 days.</p>
    )
  }

  <p>Here are a few facts about me:</p>
  <ul>
    <li>Mi nombre es {identity.firstName}.</li>
    <li>Vivo en {identity.country} y trabajo como {identity.occupation}.</li>
    {
      identity.hobbies.length >= 2 && (
        <li>
          Dos de mis pasatiempos son: {identity.hobbies[0]} and {identity.hobbies[1]}
        </li>
      )
    }
  </ul>
  <p>Mis habilidades principales son:</p><p></p><ul>
    {skills.map((skill) => <li class="skill">{skill}</li>)}
  </ul>
</BaseLayout>
